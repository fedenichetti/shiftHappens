% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iov_parse.R
\name{read_iov_desiderata_specializzandi}
\alias{read_iov_desiderata_specializzandi}
\title{Parse the residents' desiderata workbook for a given target month.}
\usage{
read_iov_desiderata_specializzandi(path, sheet, target_month)
}
\arguments{
\item{path}{xlsx path.}

\item{sheet}{Sheet name to parse (e.g. "Luglio 2026").}

\item{target_month}{YYYY-MM string; the day numbers in column B are
combined with this to build absolute dates.}
}
\value{
tibble(date, dow, shift, resident, year, status, preference, raw_value).
}
\description{
Reads a single sheet (e.g. "Luglio 2026") and returns one row per
(resident × date × shift). Weekend rows in the source are duplicated:
first = GIORNO (08-20), second = NOTTE (20-08). Weekday rows are single
and correspond to NOTTE only (residents do not work daytime feriale).
}
